pre-commit:
  parallel: true
  commands:
    format-check:
      glob: "*.dart"
      run: dart format --set-exit-if-changed {staged_files}
    analyze:
      glob: "*.dart"
      run: dart analyze --fatal-infos {staged_files}
