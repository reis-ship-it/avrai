# Supabase Scripts

CLI utilities for verifying Supabase setup from the command line.

- test_supabase_connection.dart: Basic REST checks (no Flutter)
- test_supabase_flutter.dart: Uses supabase_flutter for auth/db/storage/realtime

Run examples:
```bash
dart run scripts/supabase/test_supabase_connection.dart
```


