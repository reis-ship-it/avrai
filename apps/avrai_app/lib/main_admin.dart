Future<void> main() async {
  throw UnsupportedError(
    'The standalone admin entrypoint moved to apps/admin_app/lib/main.dart. '
    'Run the avrai_admin_app package instead of avrai_app/lib/main_admin.dart.',
  );
}
