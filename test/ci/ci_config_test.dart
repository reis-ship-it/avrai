import 'package:flutter_test/flutter_test.dart';

void main() {
  group('CI config sanity', () {
    test('env-driven SupabaseConfig is acceptable when empty in CI', () {
      // This test is a placeholder to ensure CI runs a Dart test successfully in this suite.
      expect(true, isTrue);
    });
  });
}


