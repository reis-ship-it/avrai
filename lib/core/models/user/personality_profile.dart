/// PersonalityProfile compatibility export
///
/// `PersonalityProfile` is defined in `package:avrai_ai`, but parts of the app
/// (and sibling packages like `avrai_knot`) historically imported it from:
/// `package:avrai/core/models/user/personality_profile.dart`.
///
/// This file preserves that stable import path.
library;
export 'package:avrai_core/models/personality_profile.dart';

